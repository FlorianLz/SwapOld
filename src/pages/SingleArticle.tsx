import {Text, View} from 'react-native';
import React from 'react';

export default function SingleArticle({session}: {session: any}) {
  return (
    <View>
      <Text>SingleArticle</Text>
      <View></View>
    </View>
  );
}
